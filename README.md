# Samsung-Custom-Screenshot-App-Suffix
A One UI 8 module that prevents empty screenshot filename suffixes caused by non-ASCII character stripping, enabling customizable fallback names for affected apps.
